---
import { getEntry } from 'astro:content'
import Layout from '../layouts/Layout.astro'

const page = 'implementations'
const entry = await getEntry('pages', 'implementations')
const { Content } = await entry.render()
---

<Layout title={entry.data.title} description={entry.data.description} url={`/${page}`}>
  <Content />
</Layout>
